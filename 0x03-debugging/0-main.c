#include "main.h"
/**
 * main - tests the that checks if the number is a positive or negeative integer
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
