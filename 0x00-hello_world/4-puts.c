#include <stdio.h>
/**
 * main -Its the main part that prints the line with puts function
 * Return: :0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
